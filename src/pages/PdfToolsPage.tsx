
import React from 'react';
import { Layout } from '@/components/Layout';
import { PdfTools } from '@/components/tools/text/PdfTools';

const PdfToolsPage = () => {
  return (
    <Layout>
      <PdfTools />
    </Layout>
  );
};

export default PdfToolsPage;
